type SupportTier = {
    title: string,
    description: string,
    cost: number,
    supportTierId: number,
}

type PostSupportTier = {
    title: string,
    description: string,
    cost: number,
}